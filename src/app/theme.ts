import { createTheme } from '@mui/material/styles'

const theme = createTheme({
    palette: {
        primary: {
            main: '#F31515',
        }
    },
    typography: {
        fontFamily: '"Open Sans", "Train One", sans-serif',
    },
})

export default theme
